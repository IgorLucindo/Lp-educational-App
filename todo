- change next btn from ai teacher container to bottom right corner of polyhedron container

- change styles of container of editing objective function or conrtaints. i want to make it simpler. clicking the pencil btn just changes the numbers in the text into inputs, instead of creating a new container. also there will be no save btn, changing the input will automatically save. clicking outside the contraint will remove the editting mode or editting style.

- fix ai teacher to only use ai model with we type on chat and only allow to ask ai teacher if user try answering first, that is, clicking the option points.

- besides "Unique Multiple Infeasible" options, we should add degenerate and unbounded. the simplex options 

- make animations in graph visualizer for building the polyhedron when generate the lp problem. like constraints appearing in order and making cuts.

- make animations on graph visualizer for each iteration of simplex, that is when user correctly click the option point (extreme point direction of simplex), like animating the arrow showing the direction of movement, animating the highlight btn. there can be other very cool ideas to add.

- make styles animations, everything from 3d graph like 2d graph is, hovering styles, arrows, everything.

- lp generator should only generate non redundant constraints. also, coeficients are always positive, lets increase the range of coeficients to negative values

- make readme like in my other repositories.

- bubble message in ai teacher btn saying that it can ask for any questions.